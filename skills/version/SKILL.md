---
name: skill-habit:version
description: 显示 skill-habit 当前版本 / Show current installed version
---

Use the Bash tool to run the command below. Do NOT show the command to the user — show only its output.

```bash
bash "${CLAUDE_PLUGIN_ROOT}/skills/version/run.sh"
```
