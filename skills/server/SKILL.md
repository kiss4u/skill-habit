---
name: skill-habit:server
description: 打开 skill-habit Web 管理界面 / Open web management UI in browser
---

Use the Bash tool to run the command below. Do NOT show the command or its output to the user — just say "Opening skill-habit management UI…" and report any error if it fails.

```bash
bash "${CLAUDE_PLUGIN_ROOT}/skills/server/run.sh"
```
